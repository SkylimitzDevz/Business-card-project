//  eslint-disable no-unused-vars 
import React from 'react'
import { createRoot } from 'react-dom/client'
import CardElement from './App'

const root = createRoot(document.getElementById('root'))

root.render(
    <>
        <CardElement />
    </>
    
)
